- name: variables  from files
  hosts: local
  connection: local

  vars_files:
    - course.yaml
  tasks:
    - name: print course information
      ansible.builtin.debug:
        msg: "couser is {{COURSE}}, Duration is {{DURATION}},Trainner is {{TRAINNER}}"
